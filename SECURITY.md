# Security

TrendLock 网页没有密钥输入、账户查询或订单接口。它只能读取公开行情、导入白名单字段的行情 JSON、计算规则、运行 PAPER 自动化和导出草稿。

- 不要提交 `.env`、API key、MCP 授权 URL、账户 ID、完整订单 ID 或余额/持仓快照。
- 导入数据限制为 5 MB，并只保留行情、K 线与简化工具名；外部评分和额外账户字段不会进入分析结果。
- `MCP`/`BINANCE_SKILL` 仅是导入方声明，不代表签名、身份或连接已由网页验证。
- 实时证据超过两分钟即失效；失败扫描不会让旧结果继续生成计划。
- 风险预算包含示例成本预留，但极端滑点、跳空、资金费、强平与平台故障仍可能造成更大损失。
- PAPER 自动化只在网页打开时取价；刷新恢复为 `HALTED`，行情超过 30 秒或连续三次失败会熔断，不应把它当作真实保护单。

未来若实现交易，必须单独设计测试网、持仓/挂单对账、交易所精度、保护单原子切换、断线恢复、每日熔断和人工确认。Binance MCP 的写操作必须逐次由用户确认；不要从当前 `DRAFT_ONLY` 或 PAPER JSON 直接构造实盘订单。

发现凭据泄露时应立即撤销；从 Git 历史中删除文本并不能使已泄露凭据恢复安全。
