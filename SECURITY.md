# Security

TrendLock 网页没有密钥输入、账户查询或订单接口。它只能读取公开行情、导入白名单字段的行情 JSON、计算规则、运行 PAPER 自动化、在浏览器本地监督用户录入的真实持仓，以及导出草稿。

- 不要提交 `.env`、API key、MCP 授权 URL、账户 ID、完整订单 ID 或余额/持仓快照。
- 导入数据限制为 5 MB，并只保留行情、K 线与简化工具名；外部评分和额外账户字段不会进入分析结果。
- `MCP`/`BINANCE_SKILL` 仅是导入方声明，不代表签名、身份或连接已由网页验证。
- 实时证据超过两分钟即失效；失败扫描不会让旧结果继续生成计划。
- 风险预算包含示例成本预留，但极端滑点、跳空、资金费、强平与平台故障仍可能造成更大损失。
- 当前默认单笔账户风险预算为 10%、初始硬止损为价格相对均价 −7%。10% 单笔账户风险非常激进，只是用户指定的策略参数，并不表示损失会被限制在 10%。
- PAPER 自动化只在网页打开时取价；刷新恢复为 `HALTED`，行情超过 30 秒或连续三次失败会熔断，不应把它当作真实保护单。
- MCP 监督任务的成交均价、数量和止损保存在当前浏览器的 `localStorage`，不会由站点主动上传。不要在公用电脑录入；停止使用后应清除该站点数据。下载普通分析 JSON 不包含监督任务。
- 监督任务启动前必须已经存在币安服务器保护单。页面停止、电脑休眠或网络中断时，旧保护单仍是唯一保护；网页不会继续提高止损。
- 待确认动作在提交前若被价格回撤穿越会被废弃并进入对账。新保护单只有经 MCP 读回确认后才能记入页面，不能仅凭“请求成功”文字判断。

未来若实现常驻执行服务，必须单独设计测试网、用户数据流、持仓/挂单对账、交易所精度、保护单安全切换、断线恢复、每日熔断和人工确认。Binance MCP 的写操作必须逐次由用户确认；不要从当前 `DRAFT_ONLY` 或 PAPER JSON 直接构造实盘订单。

发现凭据泄露时应立即撤销；从 Git 历史中删除文本并不能使已泄露凭据恢复安全。
